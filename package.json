{
  "name": "demo-proto-import-issue",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run clean && npm run buildproto && tsc",
    "buildproto": "./buildproto.sh",
    "clean": "rm -rf dist/"
  },
  "dependencies": {
    "grpc": "^1.15.1"
  },
  "devDependencies": {
    "@types/google-protobuf": "^3.2.7",
    "google-protobuf": "^3.6.1",
    "grpc-tools": "^1.6.6",
    "ts-protoc-gen": "^0.7.7",
    "typescript": "^3.1.3"
  }
}
